# Stage 3E tracked summaries

This directory is reserved for sanitized, generated Stage 3E aggregates:

- `matrix.json`
- `table.csv`
- `comparisons.json`
- `data-quality.json`

Do not add raw prompts, request JSONL, logs, backend URLs, process listings, credentials, or the historical `remote-results/` tree. Generate these files only from a clean committed revision after the focused GPU matrix is approved and completed.
